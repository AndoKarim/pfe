<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="aaa.pfe.auth">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.passface.PassFaceActivity" />
        <activity android:name=".view.SchemePatternActivity" />
        <activity
            android:name=".view.passface.PassFaceAdminActivity"
            android:label="@string/title_activity_pass_face_admin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".view.pincode.PinCodeActivity" />
        <activity android:name=".view.pincode.PinCodeAdminActivity"></activity>
        <activity android:name="aaa.pfe.auth.view.mother.AdminActivity"></activity>

    </application>
</manifest>